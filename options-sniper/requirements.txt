# Everything except the analyst runs on this one package. history.py reads
# Yahoo's chart endpoint with it directly, so backtesting needs no extra
# dependency — pandas and numpy were being pulled in to unpack a DataFrame
# straight back into dicts.
requests>=2.31

# Analyst layer only (USE_ANALYST=1); analyst.py imports it lazily too.
anthropic>=1.0

# Tests.
pytest>=8.0
