# بوت تيليجرام لمسح الأسهم والأوبشن (Call + Put) والعملات الرقمية 📊

بوت بثلاث وحدات **مستقلة تماماً** عن بعضها، تعمل فقط عند الطلب اليدوي (لا مسح تلقائي مستمر في الخلفية):

- 📈 **الأسهم** (`/stocks`) — إشارات ارتداد صعودي فنية، باحتمالية مبنية على نظام نقاط
- 📊 **الأوبشن** (`/options`, `/options_calls`, `/options_puts`) — عقود Call و Put معاً، باحتمالية ربح إحصائية حقيقية (Black-Scholes) وقيمة متوقعة (EV)
- 🪙 **الكريبتو** (`/crypto`) — نفس منطق الأسهم على أعلى ~100 عملة بالقيمة السوقية

كل وحدة لها **قائمة مراقبة خاصة بها**، **فلاتر خاصة بها**، ومعالجة أخطاء مستقلة تماماً — فشل وحدة واحدة (مثلاً انقطاع مزوّد بيانات) لا يوقف الوحدتين الأخريين إطلاقاً. فشل جلب سهم واحد أثناء الفحص لا يوقف بقية القائمة.

كل نتيجة تُرسل في **رسالة منفصلة خاصة بها**: الأسهم والكريبتو ترسل صورة شمعية (رسم بياني) مع ملخص وشرح كتعليق عليها، والأوبشن يرسل جدولاً نصياً (monospace) لكل عقد مع شرح.

## آلية الجلسات

كل أمر يبدأ **جلسة فحص واحدة لكل محادثة**:

- تتوقف تلقائياً بعد **15 دقيقة** كحد أقصى (`SESSION_TIMEOUT_SECONDS`) وترسل "⏰ انتهت الجلسة - أرسل أمر جديد"
- `/stop` يوقفها فوراً في أي لحظة
- لا يمكن بدء جلسة جديدة أثناء تنفيذ جلسة أخرى لنفس المحادثة

## الوصول: مقفل على الأعضاء الحاليين فقط

البوت **لا يحتوي أي أمر لإضافة أعضاء جدد**. الوصول مقتصر على قائمة ثابتة من معرّفات المحادثات المعتمدة مسبقاً، محفوظة في ملف الحالة (`state.json`، الحقل `approved`). لإضافة أو إزالة عضو يجب تعديل هذا الملف يدوياً على السيرفر.

كل نتيجة تُرسل مذيّلة بـ "⚠️ تقديرات إحصائية وليست ضمانًا".

## الوحدة 1: الأسهم (`/stocks`)

تفحص **STOCKS_WATCHLIST** (S&P 500 + Nasdaq تقريباً، ~290 سهماً، قائمة ثابتة في `scanner/config.py`) على فريم يومي، وتتطلب تحقق **3 من 4** فلاتر:

1. 📉 السعر عند الحد السفلي لبولينجر باند (ضمن 2%)
2. 🔻 RSI أقل من 35 (فترة 14)
3. 🛡️ السعر عند منطقة دعم (قاع سابق خلال 60 يوماً، هامش 3%)
4. 📐 وتد هابط، مكتمل أو شبه مكتمل

**احتمالية الربح هنا نظام نقاط مرجّح (0-85)، وليست احتمالية إحصائية** (لا يوجد سوق خيارات على السهم نفسه لحساب ذلك):

| المعيار | قوي | ضعيف |
|---|---|---|
| RSI | أقل من 30: +25 | 30-35: +15 |
| بولينجر | ضمن 1% من الحد السفلي: +25 | ضمن 2%: +15 |
| الدعم | اختُبر 3+ مرات خلال 60 يوم: +25 | اختُبر مرتين: +15 |
| الوتد الهابط | مكتمل: +25 | شبه مكتمل: +10 |

ثم تعديل حسب اتجاه السوق العام: **SPY فوق متوسطه الحركي 50 يوماً → ×1.1**، وإلا **×0.90**. الناتج يُحدّ عند 85% كحد أقصى (`STOCKS_SCORE_CAP`).

النتيجة: أفضل 5 أسهم تتجاوز **45%** (`STOCKS_MIN_POP`)، كل واحد برسالة مستقلة (صورة شمعية + بولينجر + دعم/مقاومة)، مع 🎯 الاحتمالية بارزة، والهدف السعري (أقرب مقاومة) كسياق، وشرح مختصر.

## الوحدة 2: الأوبشن — Call و Put (`/options`, `/options_calls`, `/options_puts`, `/options TICKER`)

تفحص **OPTIONS_WATCHLIST** (قائمة منفصلة تماماً — ~500-600 من أكثر الأسهم نشاطاً بعقود الأوبشن) بحثاً عن عقود **Call و/أو Put** تحقق **كل** الشروط التالية دفعة واحدة:

- |دلتا| بين 0.40 و1.0 (فعلياً بلا حد أعلى، Black-Scholes عبر `scipy.stats.norm`)
- أيام حتى الانتهاء (DTE) بين 14 و360 (يشمل LEAPS)
- حجم تداول ≥ 30 وعقود مفتوحة ≥ 200
- تقلب ضمني (IV) أقل من 60%
- سبريد عرض/طلب أقل من 10%
- سعر الطلب (ask) بين 0.05$ و1.50$ للسهم (تكلفة العقد = ask × 100)

### احتمالية الربح (POP) — إحصائية حقيقية هنا

```
Call:  POP = N(d2)
Put:   POP = N(-d2)
d2 = [ln(S/B) + (r - 0.5σ²)T] / (σ√T)
```
S = السعر الحالي، B = نقطة التعادل (Call: strike+premium / Put: strike-premium)، σ = التقلب الضمني للعقد، T = DTE/365، r = 0.045. `N` هي `scipy.stats.norm.cdf`.

### القيمة المتوقعة (EV)
```
EV = (POP × متوسط الربح المحتمل) - ((1-POP) × أقصى خسارة)
```
متوسط الربح المحتمل = الربح الصافي المتوقع (Black-Scholes) لو وصل السهم لأقرب **مقاومة** (Call) أو أقرب **دعم** (Put) عند الانتهاء. أقصى خسارة = كامل البريميوم المدفوع. EV موجب يُعلَّم بـ "📈 قيمة متوقعة إيجابية".

### الأوامر
- `/options` — كول وبوت معاً
- `/options_calls` — كول فقط
- `/options_puts` — بوت فقط
- `/options <رمز>` — سهم واحد فقط (كول وبوت معاً)، بمعزل عن بقية القائمة

النتيجة: أفضل 5 عقود بأعلى احتمالية ربح (وعند التساوي، الأطول مدة أولاً)، فوق **30%** (`OPTIONS_MIN_POP`)، كل عقد برسالة مستقلة (جدول نصي) تتضمن:

- 🟢 CALL / 🔴 PUT (نوع الرهان)
- تصنيف الاحتمالية: 🥇 ممتاز-نادر (≥40%) / 🥈 جيد جداً (35-40%) / 🥉 مقبول (30-35%)
- وسم المدة: 🕐 قصير (14-45 يوم) / 📅 متوسط (46-120 يوم، المنطقة المريحة) / 🗓️ طويل-LEAPS (121-360 يوم)
- strike، تاريخ الانتهاء، التكلفة، نقطة التعادل، الدلتا، IV، القيمة المتوقعة، واحتمالية الربح

**لماذا scipy الآن (بعد تجنبها سابقاً)؟** `scipy.stats.norm` مكتبة مستقرة جداً وواسعة الانتشار، بلا أدوات بناء C معقدة وقت التثبيت (بخلاف بعض مكتبات pricing المتخصصة) — نشر سابق لهذا البوت تعطّل لأسابيع بسبب سطرين غريبين تسللا لملف `requirements.txt` بعد دمج (merge) لم يُلاحظ، لا بسبب scipy نفسها؛ استخدامها هنا طلب صريح ومدروس.

### فحوصات سلامة البيانات (قبل أي حساب احتمالية)

كل عقد يمر بثلاث فحوصات في `scanner/options.py` قبل أن يصل لحسابات POP/EV إطلاقاً -- عقد فاشل بأي منها **يُستبعد نهائياً ولا يظهر بالنتائج**:

1. **تقلب ضمني (IV) خارج النطاق المعقول**: أقل من 10% أو أعلى من 300% → بيانات فاسدة من المصدر
2. **تضارب سعر/دلتا**: عقد سعره أقل من 0.30$ لكن دلتاه فوق 0.50 (تضارب داخلي — سعر بهذا الانخفاض لا يتوافق مع دلتا بهذا الارتفاع)
3. **تضارب السعر النظري**: لا يوجد لدى Yahoo/CBOE دلتا مصدرية مستقلة أصلاً (الدلتا هنا محسوبة دائماً من IV عبر Black-Scholes، فمقارنتها بنفسها لا فائدة منها) — الفحص المكافئ الفعلي هو مقارنة **السعر النظري** (Black-Scholes من نفس مدخلات العقد) بالسعر المعروض فعلياً؛ فرق نسبي أكبر من 50% يعني أن قيمة IV نفسها غير متسقة مع السعر الذي جاءت منه

عند انتهاء أي جلسة أوبشن (`/options`, `/options_calls`, `/options_puts`, `/options TICKER`, `/leaps`) فيها عقود مستبعدة لهذا السبب، تُضاف رسالة "⚠️ استُبعد X عقد لبيانات غير موثوقة" في نهاية الرسائل — منفصلة تماماً عن الاستبعاد العادي بسبب ضعف السيولة (الذي لا يُعد مشكلة بيانات).

### `/leaps` — عقود CALL طويلة الأجل فقط

فلتر مستقل تماماً عن `/options` أعلاه (لا يشترط سيولة/سبريد/سعر طلب معينة، ولا يُقيَّم باحتمالية الربح كمعيار قبول):

- عقود **CALL فقط**
- أيام حتى الانتهاء **365+** (`LEAPS_DTE_MIN`)
- دلتا **≥ 0.60** (`LEAPS_DELTA_MIN`، بلا حد أعلى)
- تقلب ضمني **< 0.35** (`LEAPS_IV_MAX`)
- على أسهم من OPTIONS_WATCHLIST سعرها بين **8$ و100$** (`LEAPS_MIN_PRICE`/`LEAPS_MAX_PRICE`)
- تكلفة العقد الكامل (premium × 100) لا تتجاوز **170$** (`LEAPS_MAX_COST`)

النتيجة: أفضل 5 عقود **مرتبة بأقل تقلب ضمني (IV) أولاً** — وليس أعلى احتمالية ربح كباقي أوامر الأوبشن — كل عقد برسالة مستقلة (جدول نصي) مع IV بارزاً واحتمالية الربح كسياق داعم فقط.

## الوحدة 3: الكريبتو (`/crypto`)

تفحص **CRYPTO_WATCHLIST** (أعلى ~100 عملة بالقيمة السوقية، أزواج USDT على Binance) على فريم **4 ساعات** عبر بيانات Binance العامة (`ccxt`، بلا مفاتيح API)، وتتطلب تحقق **2 من 3** فلاتر (بلا وتد هابط، خاص بالأسهم فقط):

1. 📉 بولينجر السفلي (ضمن 2%)
2. 🔻 RSI أقل من 35
3. 🛡️ منطقة دعم (آخر 30 يوماً = 180 شمعة 4 ساعات، هامش 3%)

نفس نظام نقاط وحدة الأسهم (RSI/بولينجر/دعم قوي/ضعيف)، بالإضافة لنقطة رابعة: **حجم تداول شرائي متزايد آخر 12 ساعة: +20**. التعديل حسب الاتجاه: **BTC فوق متوسطه الحركي 50 يوماً → ×1.1**، وإلا **×0.85**. الحد الأقصى **75%** (`CRYPTO_SCORE_CAP`).

النتيجة: أفضل 5 عملات فوق **40%** (`CRYPTO_MIN_POP`)، بنفس أسلوب العرض (رسالة مستقلة بصورة رسم بياني وشرح)، مع **💧 السيولة**: حجم التداول الفعلي بالدولار (USDT) خلال آخر 24 ساعة من بيانات Binance المباشرة.

## أوامر البوت

| الأمر | الوظيفة |
|---|---|
| `/stocks` | فحص وحدة الأسهم (أفضل 5 نتائج) |
| `/options` | فحص وحدة الأوبشن كاملة، Call + Put معاً (أفضل 5 عقود) |
| `/options_calls` | فحص عقود Call فقط |
| `/options_puts` | فحص عقود Put فقط |
| `/options <رمز>` | فحص عقود سهم واحد (Call + Put معاً) |
| `/leaps` | عقود CALL طويلة الأجل (LEAPS، 365+ يوم) فقط |
| `/crypto` | فحص وحدة الكريبتو (أفضل 5 نتائج) |
| `/stop` | إيقاف الجلسة الحالية فوراً |
| `/status` | حالة عضويتك، جلستك الحالية، والسوق، وآخر نتائج فحص |

لا توجد أوامر أخرى — لا اشتراك مدفوع، لا إدارة أعضاء، لا مسح تلقائي.

## مصادر البيانات

- قائمتا الأسهم والأوبشن: قوائم ثابتة قابلة للتعديل في `scanner/config.py`
- الأسعار/الأوبشن: Yahoo Finance عبر `yfinance` (احتياطي لسلاسل العقود عبر CBOE عند تعطل Yahoo)، لكل من Call وPut
- الكريبتو: Binance العامة عبر `ccxt` (بيانات سوق فقط، بلا مفاتيح API)
- الرسوم البيانية: `matplotlib`/`mplfinance` (تُرسم فقط لأفضل 5 نتائج نهائية، ليس لكل مرشّح أثناء الفحص)

## التشغيل

### 1. إنشاء البوت
راسل [@BotFather](https://t.me/BotFather) في تيليجرام، أرسل `/newbot`، واحفظ التوكن.

### 2. الإعداد
```bash
git clone <repo-url> && cd cloud
pip install -r requirements.txt
cp .env.example .env    # ثم ضع التوكن داخل .env
```

### 3. اعتماد الأعضاء
أضف معرّفات المحادثات المسموح لها يدوياً في ملف الحالة (`state.json` داخل `DATA_DIR`):
```json
{"approved": {"123456789": 0}}
```
`0` = اشتراك دائم، أو رقم Unix timestamp لتاريخ الانتهاء.

### 4. التشغيل
```bash
python bot.py
```

> **ملاحظة تسمية**: نقطة الدخول اسمها `bot.py` وليس `main.py` — `main.py` في هذا المستودع يخص أداة أخرى مستقلة تماماً (`options_scanner/cli.py`)، فلم نستبدله تفادياً لكسرها.

⚠️ البوت لا يحتاج تشغيلاً متواصلاً 24/7 بالضرورة (لا مسح خلفي) لكن يُفضّل تشغيله على سيرفر دائم (VPS، Railway، Render...) ليستجيب للأوامر في أي وقت.

### النشر على Railway

1. اربط المستودع، وتأكد أن الفرع المنشور هو الفرع الذي يحوي الكود
2. في تبويب **Variables** أضف `TELEGRAM_BOT_TOKEN`
3. **فاليوم دائم (مهم)**: من قائمة الخدمة اختر **Attach Volume** وحدد مسار التركيب مثلاً `/data` — البوت يكتشف الفاليوم تلقائياً (عبر `RAILWAY_VOLUME_MOUNT_PATH`) ويحفظ فيه ملف الأعضاء المعتمدين

على أي استضافة أخرى: عيّن `DATA_DIR=/path/to/persistent/dir` لنفس الغرض.

## متغيرات البيئة الرئيسية

| المتغيّر | الافتراضي | الوصف |
|---|---|---|
| `TELEGRAM_BOT_TOKEN` | — | توكن البوت (مطلوب) |
| `ADMIN_CHAT_ID` | 0 | معرّفك الشخصي (مصرّح له دائماً) |
| `SESSION_TIMEOUT_SECONDS` | 900 | مهلة أي جلسة فحص (15 دقيقة) |
| `STOCKS_FILTERS_REQUIRED` | 3 | من أصل 4 فلاتر أسهم |
| `STOCKS_MIN_POP` | 45 | أقل نقاط احتمالية أسهم لعرض النتيجة |
| `STOCKS_SCORE_CAP` | 85 | الحد الأقصى لنقاط الأسهم |
| `CRYPTO_FILTERS_REQUIRED` | 2 | من أصل 3 فلاتر كريبتو |
| `CRYPTO_MIN_POP` | 40 | أقل نقاط احتمالية كريبتو لعرض النتيجة |
| `CRYPTO_SCORE_CAP` | 75 | الحد الأقصى لنقاط الكريبتو |
| `OPTIONS_DELTA_MIN` / `OPTIONS_DELTA_MAX` | 0.40 / 1.0 | نطاق الدلتا المقبول (فعلياً بلا حد أعلى) |
| `OPTIONS_DTE_MIN` / `OPTIONS_DTE_MAX` | 14 / 360 | نطاق الأيام حتى الانتهاء (يشمل LEAPS) |
| `OPTIONS_ASK_MIN` / `OPTIONS_ASK_MAX` | 0.05 / 2.00 | نطاق سعر الطلب للسهم الواحد |
| `OPTIONS_MIN_POP` | 30 | أقل احتمالية ربح مقبولة (وحد "البرونزي") |
| `OPTIONS_TIER_GOLD` / `OPTIONS_TIER_SILVER` | 40 / 35 | حدود تصنيف 🥇/🥈 |
| `LEAPS_DTE_MIN` | 365 | أقل DTE لعقود `/leaps` |
| `LEAPS_DELTA_MIN` | 0.60 | أقل دلتا لعقود `/leaps` |
| `LEAPS_IV_MAX` | 0.35 | أعلى تقلب ضمني لعقود `/leaps` |
| `LEAPS_MIN_PRICE` / `LEAPS_MAX_PRICE` | 8 / 100 | نطاق سعر السهم لعقود `/leaps` |
| `LEAPS_MAX_COST` | 170 | أعلى تكلفة عقد كامل لعقود `/leaps` (بالدولار) |
| `DATA_DIR` | . | مجلد حفظ `state.json` |

القائمة الكاملة للمتغيرات (نقاط التسجيل، أوزان الاتجاه، إلخ) موجودة كتعليقات في `scanner/config.py`.

## هيكل المشروع

```
bot.py                       # نقطة الدخول: الأوامر السبعة + آلية الجلسات (15 دقيقة + /stop)
main.py                      # (غير هذا البوت) نقطة دخول أداة options_scanner/cli.py المستقلة
scanner/
  config.py                   # فلاتر ونقاط ثلاث الوحدات + القوائم + إعدادات عامة
  state.py                    # قائمة الأعضاء المعتمدين الثابتة (state.json)
  utils.py                     # تنسيق نصوص عام (أسعار، حجم تداول، تقسيم رسائل طويلة)
  indicators.py                 # بولينجر/RSI/الدعم (مع عدد مرات الاختبار)/المقاومة/الوتد الهابط (بدرجات)
  data.py                      # تنزيل OHLCV دفعي عبر yfinance (أسهم/أوبشن)
  crypto_data.py               # OHLCV + سيولة 24 ساعة عبر ccxt/Binance العامة
  chart.py                      # رسم بياني شمعي (بولينجر + دعم/مقاومة) لأفضل النتائج
  options.py                  # جلب سلاسل عقود Call وPut (Yahoo + CBOE) + دلتا/ثيتا (scipy)
  probability_module.py         # Black-Scholes كامل: تسعير نظري، POP (Call/Put)، EV
  market_calendar.py            # هل السوق الأمريكي مفتوح الآن (لعرض /status فقط)
  stocks_module.py              # وحدة الأسهم (مستقلة، نظام نقاط)
  options_module.py             # وحدة الأوبشن (مستقلة، Call+Put، POP إحصائي + EV + LEAPS)
  crypto_module.py              # وحدة الكريبتو (مستقلة، نظام نقاط)
```
